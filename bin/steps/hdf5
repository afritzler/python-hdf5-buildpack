# Add HDF5 libraries to path
echo "Adding HDF5 library support"
mkdir -p .heroku/vendor
tar xzf $ROOT_DIR/vendor/hdf5.tar.gz -C .heroku/vendor/

echo "Checking what vendor directory look like"
ls -l $BUILD_DIR/vendor/
